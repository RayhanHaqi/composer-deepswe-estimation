# Notebooks

Optional exploratory notebooks can live here. The canonical reproducible path is the `scripts/` CLI pipeline.

If you add a notebook, document the exact data snapshot and git commit used.
