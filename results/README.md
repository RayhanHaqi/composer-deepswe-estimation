# Results

`estimate_composer.py` writes:

- `estimates.csv` — per-method point estimates and assumptions
- `summary.json` — min/max/mean/median across methods
- `overlap_pairs.csv` — matched CursorBench ↔ DeepSWE rows used for linking
